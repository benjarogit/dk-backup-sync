# Python
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
.Python

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Addon-ZIPs nur unter repo/output/ (Build-Artefakte); keine ZIPs in addons/ committen
addons/**/*.zip

# repo/output/ is NOT ignored – it is committed so GitHub can serve addons.xml and ZIPs.

# Development tools and docs (local only, not on GitHub)
gitpush.sh
ENTWICKLUNG.md
tools/
# Lokaler Symlink .cursor/plans -> ~/.cursor/plans (Build-Button Workaround)
.cursor/plans
# Kodi-Quellcode (Referenz-Klon, nur zum Nachschlagen)
kodi-xbmc/

# IDE / lokale Konfiguration (nicht für User)
.cursor/
.vscode/
.vale.ini
*.code-workspace

# Lokale Docs/Referenz (nicht für User)
ANLEITUNG.md
docs/
tests/

# Fremd-Addon-ZIPs (nur zum Anschauen, nicht ins Repo)
ParanoidWizard-DE.zip
plugin.program.openwizard-*.zip
plugin.program.super.favourites-*.zip
script.ezmaintenanceplus-*.zip
