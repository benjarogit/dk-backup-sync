# Python
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
.Python

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Addon-ZIPs in addons/ nicht committen (Build-Artefakte landen in dist/)
addons/**/*.zip

# dist/ wird committed (Repository-Pakete für GitHub)

# Development tools (local only, not on GitHub)
gitpush.sh
ENTWICKLUNG.md
tools/
docs/
changelog_entry.sh
repo/DEPLOY.md
repo/.env
.env.dkrepo
# Lokaler Symlink .cursor/plans -> ~/.cursor/plans (Build-Button Workaround)
.cursor/plans
# Kodi-Quellcode (Referenz-Klon, nur zum Nachschlagen)
kodi-xbmc/

# IDE / lokale Konfiguration (nicht für User)
.cursor/
.vscode/
.vale.ini
*.code-workspace

# Lokale Referenz (nicht für User)
ANLEITUNG.md
tests/

# Fremd-Addon-ZIPs (nur zum Anschauen, nicht ins Repo)
ParanoidWizard-DE.zip
plugin.program.openwizard-*.zip
plugin.program.super.favourites-*.zip
script.ezmaintenanceplus-*.zip
